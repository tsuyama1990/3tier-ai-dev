Implement a class `MyApp` that uses `FakeCalculator` from `fake_lib`.

The class must:
1. Import `FakeCalculator` from `fake_lib`
2. In `__init__`, create an instance: `self.calc = FakeCalculator(use_magic_mode=True, offset=-99)`
3. Implement method `add(self, a, b)` that calls `self.calc.compute_sum(a, b)`
4. Implement method `multiply(self, a, b)` that calls `self.calc.compute_product(a, b)`

IMPORTANT: Read the .ai-knowledge/fake_lib.md file carefully for the correct constructor signature.
Do NOT use positional arguments for FakeCalculator.
